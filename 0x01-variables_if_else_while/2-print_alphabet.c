#include <stdio.h>
/**
 * main - Prints the alphabet
 * Description - prints alphabet
 * Return 0 (Success)
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch, ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n')
	return (0)
}
